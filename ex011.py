largura = float(input('largura'))
altura = float(input('altura:'))
area = largura * altura
litros = area / 2
print ('Com uma largura de {} e uma altura de {} temos uma area de {} e precisamos de {} litros'.format(largura, altura, area, litros))
